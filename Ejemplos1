//////////////////////1///////////////////////
/*Ejemplo funcion factorial*/
package main

import "fmt"

// Esta función `fact` se llama a si misma hasta que llega a la
// base de `fact(0)`.

func fact(n int) int {
    if n == 0 {
        return 1
    }
    return n * fact(n-1)
}

func main() {
    fmt.Println(fact(7))
}
///////////////////////////2////////////////////
